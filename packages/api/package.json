{
  "name": "@support/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/server.ts",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:integration": "RUN_API_INTEGRATION_TESTS=true vitest run src/app.integration.test.ts src/auth.integration.test.ts src/inbound-intake-store.integration.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@support/api-client": "workspace:*",
    "@types/busboy": "^1.5.4"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/rate-limit": "^10.1.1",
    "@support/db": "workspace:*",
    "@support/integrations": "workspace:*",
    "@support/observability": "workspace:*",
    "@support/shared-schemas": "workspace:*",
    "@temporalio/client": "^1.18.1",
    "busboy": "^1.6.0",
    "fastify": "^5.0.0",
    "ioredis": "^5.4.1",
    "jose": "^6.2.3",
    "tsx": "^4.22.4",
    "zod": "^4.0.0"
  }
}
