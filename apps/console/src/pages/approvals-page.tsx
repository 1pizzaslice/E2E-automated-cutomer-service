// Approval queue → evidence → decide lands in Milestone 23 Slice 3. This
// placeholder keeps the route wired so Slice 2's navigation and permission
// gating are exercisable end to end.
export function ApprovalsPage() {
  return (
    <section>
      <h1>Approvals</h1>
      <p>The reviewer queue is coming in the next slice.</p>
    </section>
  );
}
